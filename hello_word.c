#include <stdio.h>
#include "sum_calcul.h"

int main(int argc,char argv[])
{
  int a = 2, b = 3;
  printf("Hello World\n");
  printf("Github already exist and push already done!\n");
  printf("Sum of a and b =%d\n",sum(a,b));
  printf("Mortadha Test Github !!");
  return 0;
}
